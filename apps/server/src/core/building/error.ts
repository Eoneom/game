export enum BuildingError {
  NOT_FOUND = 'building:not-found',
  ALREADY_IN_PROGRESS = 'building:already-in-progress',
  NOT_IN_PROGRESS = 'building:not-in-progress',
  QUEUE_FULL = 'building:queue-full',
  QUEUE_ITEM_NOT_FOUND = 'building:queue-item-not-found'
}
